{
  "name": "fractal-rl",
  "version": "0.0.1",
  "module": "index.ts",
  "type": "module",
  "private": false,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun run build:clean && tsc",
    "build:clean": "rm -rf ./dist",
    "start": "bun run server.ts",
    "test": "bun test tests"
  },
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
